package com.uptoser.java.design_patterns.behavioral_patterns.state.group1;

/**
 * 抽象状态 (State) ：
 * 抽象状态是一个接口或抽象类。抽象状态中定义了与环境（ context ）的一个特定状态相关的若干个方法。
 */
public interface State {
//    public void handle(Context context);
    public void handle();
}
